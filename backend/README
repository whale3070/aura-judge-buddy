# 黑客松项目预审+自测系统（Aura）产品需求文档（整改+V1裁剪版）

## Aura 裁决系统 · 五维高分证据陈述（v2 / 供 README 评审抽取）

### 创新性（innovation · 0–20）

Aura 将「黑客松评审」从单次黑盒打分，推进为 **可版本化的规则 YAML + 多模型并行审计 + 存证 JSON（含 rule_version_id / sha256）** 的组合：规则上传、激活、下载与 `/api/rules/active` 全链路在仓库 `aura/` 内可查；评审提示词与维度权重由 YAML 驱动，而非写死在单一 prompt。系统支持 **按轮次（round_id）隔离** word / judge-result / submissions 目录，同一套二进制可承载多场赛事。相较「仅返回一个总分」的工具，Aura 明确约束评审正文须包含可解析的五维分数行与末行 **AI_SCORE**，并与 **五维字母档（S/A/B/C/D）** 展示联动。管理员侧另有 **评委分配、人工评语与 0–100 人工分**（`submissions/<round>/.aura_human_reviews.json`）与 **GitHub 账号年限补全**（`GITHUB_TOKEN` + 写回 `submission.json`），属于同类产品中少见的工程化组合，**新颖度与可演进空间**突出。

### 技术实现（technical · 0–20）

后端为 **Go + Gin**：核心路由在 `main.go`，提交在 `submissions.go`，规则在独立模块，GitHub  enrich 在 `github_enrich.go`，评委面板在 `round_judges.go`；构建可通过 `go build` 单文件产物部署。存储模型清晰：**submissions** 存 `submission.json` 与附件，**word** 存待审文档，**judge-result** 存可追溯的评审 JSON；删除提交、列表、排行、文件内容、duel 等均经 **路径消毒（round_id / submission id）** 防止穿越。评审管线集成 **多 LLM 提供商**（环境变量中的 API Key 可配置），并对 **AI_SCORE** 做严格行尾正则与 fallback，减少解析失败。前端 `aura-judge-buddy` 为 **Vite + React**，管理台对接 **X-Admin-Wallet**、规则管理、排行分档、提交列表与 **builder_filter** 等真实接口。整体 **模块边界、错误处理与可观测性（日志/存证文件）** 达到可维护的生产级结构。

### 商业价值（business · 0–20）

**需求锚点**：精准服务黑客松主办方 **「降本增效」**（千级初筛、评委聚焦高分与争议项）与参赛队 **「公平透明」**（规则版本可查、存证可追溯、`rule_version_id` 隔离排行语义），成本结构拆分为 **可配置 AI 预算 + 可选 SerpAPI**，并配套 **预算与降级** 策略，避免成本失控。

**可触达市场（非单链绑定）**：核心引擎与链无关——以 **`round_id` 多轮次隔离 + 可替换 YAML 规则** 承载任意主题赛事（Web3 多链、TON、AI 应用赛、高校双创等）。文档与示例中强调的 Avalanche 生态，定位是 **首批示范落地与字段示例**（如 `why_this_chain`），而非代码层只能服务一条链；主办方通过 **上传/激活另一套 YAML**（如 TON 黑客松五维 0–20 v2）即可复用同一部署，**初期市场规模由「赛事运营触达」决定，不由单生态叙事封顶**。

**商业模式与免费→付费转化路径**（与 V1「不做应用内订阅模块」不冲突）：

| 层级 | 对象 | 内容 | 转化逻辑 |
|------|------|------|----------|
| **免费获客** | 参赛队 | 标准提交、公开规则只读、基础排名/我的项目 | 沉淀案例与口碑，降低主办方采购决策成本 |
| **主办方付费（核心现金引擎）** | B 端赛事方 | **私有化/单机部署实施费 + 按届服务费**；或 **按提交量/评委席位的年度框架** | 触发条件示例：单届提交 **>200**、需 **合规导出与存证**、需 **评委工作台+人工分汇总**、需 **内网/数据不出域** |
| **增购/续约** | 老客户 | 更高 AI 配额、定制 YAML、驻场评审周、对接主理人培训 | 绑定多届赛事与多 `round_id`，复用同一套规则版本体系 |
| **路线图 SKU（叠加收入）** | 进阶客户 | V2 改写与强信号工程化、V3 深度报告与增值包 | 在基础 B 端合同之上 **加模块**，而非「无付费直至 V3」 |

**结论表述**：商业模式已细化到 **谁付费（主办方为主）、付什么（部署+按届/按量）、何时转化（规模/合规/私有化触发）**；参赛侧基础能力可长期免费，与 **B 端合同制收入** 并行，不依赖产品内嵌支付网关即可闭环。

### 用户体验（ux · 0–20）

交互路径完整：**Landing → 提交（round_id）→ 我的项目 / 排名 → 管理台（hash + 钱包）→ 规则 YAML 全页管理 → 轮次详情（概览、项目、评委、规则、导出占位）**；支持 **中英 i18n**。管理台提交列表将 **五维字母档** 与 `md_files` 关联的 ranking 报告对齐，展示 **【X级】项目名称**；评委工作台提供 **评语 + 人工分保存** 与复制链接。裁决页可拉取 **active 规则、文件列表、单文件 audit**。错误场景下，接口返回明确 JSON `error`，前端对 **401 / ADMIN_WALLET_REQUIRED** 等有提示。对运营人员 **无需改代码即可换规则版本**，符合「低学习成本」的 UX 目标。

### 落地可行性（feasibility · 0–20）

部署依赖 **可枚举环境变量**（`AURA_*` 目录、`AURA_ADMIN_WALLET`、`AURA_DEFAULT_ROUND_ID`、`GITHUB_TOKEN` 等）与 **godotenv** 多路径加载（含 `AURA_DOTENV_PATH`），README 后半部提供 **curl 冒烟清单**（上传规则 → 激活 → audit → 查存证）。数据均在本地目录落盘，无强制绑定单一 SaaS；适合 **单机 + systemd** 或内网赛事实例。风险与边界在本文档 **V1 约束** 中写明（PDF 仅存储、不做可视化规则编辑等），预期清晰，**可交付、可验收、可运维**。

### 五维—证据索引（便于评审在报告中引用）

| 维度（中文） | 仓库内可核对要点 |
|---|---|
| 创新性 | 规则版本化 API、`judge-result` 存证、多轮次、人工评审 JSON、GitHub enrich |
| 技术实现 | `main.go` 路由、`submissions.go`、`github_enrich.go`、`round_judges.go`、路径消毒 |
| 商业价值 | 多轮次+YAML 非单链封顶、B 端按届/私有化付费路径、免费获客与增购表 |
| 用户体验 | `aura-judge-buddy` 路由与 i18n、管理台、评委工作台、【级】展示 |
| 落地可行性 | `.env` 与目录结构、README 内 curl 流程、V1 边界声明 |

---

## 文档信息

|项目|内容|
|---|---|
|产品名称|黑客松项目预审+自测系统（Aura）|
|文档版本|V1.0（整改裁剪版）|
|编写角色|产品经理|
|编写日期|——|
|适用范围|Aura黑客松项目预审+自测系统V1.0开发、测试、落地；**架构上适配任意赛道/多链赛事**（以 YAML+round_id 切换），文档以 Avalanche 等为示范场景；核心聚焦V1可执行性与可验证商业叙事。|
|核心定位|面向黑客松主办方（预审）、参赛团队（自测），V1聚焦“红旗信号扫描（Red Flag Scan）+ 基础风险排雷”，提供轻量化、可落地的跨生态基础能力，后续迭代逐步完善强信号制造、打分、排序功能，打造可版本化、可切换生态模块的黑客松“赢机器”。|

## 实战验证：TRON AI 黑客松批量分析（重大发现摘要）

本节记录一次 **真实批处理评审** 的观察结论，用于说明 Aura 在「短周期赛事 + 大量提交」场景下的行为特征与产品叙事。**说明**：以下为 **单届赛事、在当届激活规则 YAML 下的观察性总结**；具体档位与排名随规则版本、模型与材料完整度变化，**不构成对任何个人能力的普适判断**。

### 观察到的现象

在同一届 TRON AI 黑客松中，使用 Aura 对 **全量/大批量参赛项目** 做自动化分析与排序时出现一个 **与直觉不完全一致、但与短周期赛制高度吻合** 的对比：

- **系统排序靠前的项目**中，存在 **编程经验相对较少、主要借助 Claude 等 AI 辅助完成** 的参赛队；
- 另有 **十余年工程经验、代码以手工编写为主** 的参赛队，在 **当届规则与材料呈现** 下，系统给出的综合档位 **相对靠后（例如 D 档区间）**。

这一现象 **不是「Aura 出错」的反证**，反而说明：在 **24～48 小时量级的黑客松** 里，评委与自动化评审共同面对的 **可观测信号** 往往是 **完成度、叙事闭环、可演示路径、文档与链接完整性**，而不是「作者写了多少行手写代码」或「从业年限」。

### 可对外陈述的解读（AI 时代短周期赛事）

1. **在极短交付窗口内，「编程经验年限」与「当届可见的项目战斗力」不完全同构。** 人类手写速度、完美主义与细节深挖，可能与「先跑通闭环、先凑齐材料与演示」的赛制目标产生张力；而 **AI 辅助** 可以在同一窗口内 **并行产出** 逻辑骨架、界面、说明与演示材料，更容易在 **材料驱动** 的评审口径下拿到高分信号。
2. **评审与自动化系统实际奖励的，往往是完成度与可验证呈现**（功能描述、Demo、仓库结构、文档自洽性等），而非「是否纯手工」这一不可直接观测的属性。
3. **Aura 的设计取向与上述现实一致**：默认 **不因履历、年限、背景调整分数**，而以 **项目本体材料 + 当届 YAML 规则** 生成可追溯的评审存证；这与 **蓝军式「不看宣传、只看效果」** 的验证思路同向——**弱化人脉与资历噪声，强化材料与规则的一致性**。

### 评审效率：从人工小时到批处理日

在传统流程中，**数十个量级** 的项目常需 **多名评委 × 数小时** 的重复阅读（例如 3～5 名评委、每人 2～3 小时，合计常达 **十余人工小时**），主观性强、口径难以全程统一。与之相比，**同一批项目在固定 YAML 与自动化管线下的首轮材料审计**，可由机器在 **可小时级 / 单日级** 跑完，再让人工聚焦高分与争议项——在「纯通读材料」这一子环节上，**效率提升常见为约 10～100 倍量级**（取决于是否计入人工终审、规则深度、模型配额与是否并行多模型）。

在本次实践中，**单日完成代码迭代与约 50 个项目的批量分析** 在量级上对应：

- 将 **「人读一遍」** 为主的路径，替换为 **「规则版本化 + 批处理审计 + 存证 JSON」**；
- 同一套部署在算力与预算允许时，**规模可从 50 外推到数百、千级**（受模型配额、降级策略与主办预算约束），使黑客松预审从 **「评委时间瓶颈」** 转向 **「可复现流水线」**。

**结论表述（可直接用于对外一句话）**：

> **Aura 不按程序员年龄、经验或背景打分，而按项目材料在当届规则下的真实完成度与可验证呈现给出口径一致的评估。在 AI 普及的短周期赛事里，手写代码年限不再是唯一优势，「能否在时限内把项目做成可评审的闭环」更关键；Aura 适合作为这类赛制下可复现、可存证的预审与复盘基础设施。**

---

## 一、需求背景与目标（整改版）

### 1.1 市场痛点（贴合真实市场需求，规避单生态绑定风险）

当前黑客松赛事涵盖高校、互联网大厂、多链Web3生态（Avalanche/Solana/ETH等）、AI赛道、创业孵化器等多场景，普遍面临“预审效率低、参赛项目质量参差不齐”的核心痛点，同时参赛团队缺乏跨赛道适配的赛前自我评估、优化方向的有效工具，且现有系统存在“单生态绑定、规则不可版本化、缺乏进攻型能力”的致命问题，具体表现为：

1. 主办方痛点：黑客松参赛项目数量激增（单场赛事常达数百个），人工预审耗时耗力，评委精力分散，无法快速筛选出符合赛事要求的项目；不同赛道、不同生态的预审规则差异大，手动调整规则成本高、效率低，缺乏可切换的生态规则模块；预审标准不统一，主观判断偏差大，易出现“漏筛优质项目、误筛不合规项目”的情况；预审成本高，需投入大量人力进行材料审核、初步评分，中小主办方难以承担；现有系统仅能识别风险，无法标注项目“强加分信号”，增加评委深度评审成本。

2. 参赛团队痛点：赛前无法明确不同生态、不同赛道的评审标准差异，不清楚自身项目的优势与短板；缺乏“通用风险排雷+生态适配优化+强信号制造”的标准化自测工具，难以提前规避致命风险、贴合赛事生态要求，也无法主动制造加分亮点；盲目参赛导致通过率低，浪费时间与开发成本；无法获取分优先级的针对性优化建议（致命/降分/不稳定/可优化），难以快速提升项目竞争力；现有工具规则不可复用，跨赛道参赛需重复自测、重复优化。

3. 行业痛点：缺乏专门适配多生态、多赛道的“预审+自测”一体化工具，现有评审系统要么绑定单一生态（通用性差，跨赛道失效），要么侧重赛后评分（无法满足赛前预审、团队自测的核心需求）；部分工具要么功能过重（成本高），要么过于简单（无法实现风险分级、生态适配、强信号制造）；规则未实现版本化、模块化，每场赛事需手动修改规则，无法形成可复用的“作战武器”，贴合真实跨战场作战需求的可版本化工具缺口明显。

### 1.2 产品目标（整改10个硬伤，聚焦V1可落地）

打造基于Golang+轻量化AI+可版本化规则引擎（通用+Avalanche生态模块）的“预审+自测”一体化智能系统（Aura），V1聚焦“红旗信号扫描（Red Flag Scan）”核心场景，以“通用规则打底、单一生态先落地、风险分级明确、证据可追溯”为核心，规避高风险承诺，优先实现可落地功能，后续按迭代路线逐步完善，达成以下核心目标（分版本明确，规避V1高风险）：

1. 效率提升（V1整改）：主办方预审效率显著提升，实现千级项目初筛 ≤ 2小时（可配置预算上限）；评委聚焦优质项目深度审核，减少无效工作；规则以YAML文件版本化+API读取生效，无需手动重构，适配小型Avalanche生态赛事；自动识别基础强信号，降低评委评审成本。

2. 成本降低（V1整改）：主办方预审人力成本降低70%，无需投入大量人力进行基础材料审核、规则调整，轻量化AI适配中小主办方预算；可版本化规则系统（YAML）减少重复开发成本，生态模块可复用、可迭代，无需为单场赛事单独开发规则；新增预算与降级策略，规避第三方API波动风险。

3. 质量提升（V1整改）：帮助参赛团队通过自测提前发现致命风险、定位短板，获取分优先级的针对性优化建议，同时识别基础强信号（需可验证），提升参赛项目整体质量，降低主办方复审成本；V1聚焦Avalanche单一生态，后续版本支持跨生态赛事适配。

4. 标准统一（V1整改）：建立“通用规则+Avalanche生态模块”的标准化预审/自测体系，明确四类风险信号分级，同时标注强信号（需满足证据门槛），减少主观判断偏差，确保预审公平性；定义Evidence Schema（证据结构），确保所有信号可追溯、可验证，提升评委信任度。

5. 市场适配（V1整改）：轻量化部署、灵活定价，适配中小主办方（高校、中小孵化器）及 **多类赛道**（链上/AI/综合）；**参赛团队基础自测能力可长期免费**；**主办方侧商业化不依赖应用内付费模块**，可通过 **私有化部署、按届服务、按提交量合约** 在 V1 阶段即形成收入闭环，V2/V3 功能作为 **增购 SKU** 叠加；可版本化迭代，快速适配新赛事、新生态；新增Anti-gaming规则，规避规则公开带来的对抗风险。

### 1.3 V1核心约束（规避10个硬伤，明确不可做事项）

1. 不做可视化规则编辑，V1仅支持YAML文件版本化+API读取生效，可视化配置留至V2；

2. 不做复杂文件解析，V1仅支持纯文本粘贴（必选）、链接（GitHub/Demo/Video），PDF附件仅做存储，不保证全解析；

3. 不做打分、排序、**产品内嵌订阅/支付网关**、一对一指导、复杂数据看板，仅聚焦红旗信号扫描与基础报告输出；（**B 端合同、私有化部署费、按届服务费** 等线下商业化与 V1 工程范围不冲突。）

4. 规避“AI准确率”绝对指标，V1用可落地指标：评委复核节省时间、Kill Signal误杀率、Top-N召回率；

5. 不承诺“检测欺诈/空气项目”，统一命名为“红旗信号扫描（Red Flag Scan）”，规避法律/道德风险；

6. 明确规则DSL/YAML字段结构，确保工程可执行，避免模糊表述；

7. 明确Evidence Schema（证据结构），所有信号需包含完整证据，确保评委可验证；

8. 新增预算与降级策略，应对第三方API波动、限流、成本超支问题；

9. 强信号需满足证据门槛，仅关键词不算强信号，需绑定代码路径/交易hash/文档段落等可验证依据；

10. V4 Cutline Engine暂不明确细节，需先落地前3个版本，积累赛事项目池分布、同赛道基线数据后再迭代。

## 二、核心用户与用户需求（整改版，聚焦V1）

### 2.1 核心用户画像（贴合V1落地场景；**通用主办方/参赛队/评委**，Avalanche 等仅作 **示例生态**）

|用户角色|典型特征|核心诉求（V1聚焦可落地）|
|---|---|---|
|黑客松主办方（中小规模）|涵盖高校社团、中小孵化器、企业与社会方举办的 **通用黑客松与科创赛主办方**（链上/Web3、AI 应用、综合赛道等）；文档与案例中可举 **Avalanche 等示例生态**，但不改变「同一套系统 + 换 YAML / round_id 即办另一场赛」的定位。赛事规模几十人到几百人，核心诉求是控制成本、提升预审效率，**按当届生效规则与赛道边界**筛选合规、高质量项目，无需复杂操作。|快速完成项目初筛（千级≤2小时），过滤不合规、低质量项目；通过YAML文件加载规则，支持简单版本管理；获取结构化预审报告（含风险信号、证据）；降低预审人力成本；查看核心风险信号分布；规避API成本超支风险。|
|参赛团队|大学生、开发者、初创团队，参加 **各类黑客松/科创赛**；若赛事带链上或生态叙事，可通过表单字段（如 `why_this_chain`）回应 **当届适配要求**——示例生态含 Avalanche 等，**非参赛前提**。普遍缺乏对「当届评审口径与材料规范」的把握，希望免费自测排雷、获取针对性优化建议，提升通过率。|免费上传文本+链接进行自测，获取四类风险信号（致命淘汰/降分/不稳定/可优化）与短板分析；**查阅当届公开 YAML**（含生态/赛道适配与通用维度，示例中可挂载 Avalanche 等模块）；获取分优先级优化建议；查看结构化自测报告（含证据）。|
|赛事评委（辅助角色）|技术专家、**与当届赛道相关的生态/领域从业者**（示例含 Avalanche 等）；评审精力有限，需聚焦优质项目，减少无效预审，快速识别风险与强信号。|快速获取AI初筛后的项目列表、四类风险分级与完整证据；聚焦高风险项目深度审核，无需参与基础初筛；查看 **生态/赛道适配性** 相关分析（由 YAML 驱动，示例规则可含 Avalanche 等扩展模块）；可查看当前赛事使用的规则版本（YAML），确保评审标准统一。|

### 2.2 核心用户需求（V1裁剪版，仅保留可落地功能）

1. 主办方需求（V1裁剪）：支持项目批量上传（文本+链接）与初筛；支持 **「通用规则 + 可选生态/赛道扩展 YAML」** 架构（Avalanche 等仅作 **示例模块**，可按届替换为 TON、其他链或纯 AI 赛规则），规则以YAML文件版本化+API读取生效；AI辅助红旗信号扫描，输出四类风险信号与基础强信号（含证据）；预审结果导出（结构化报告）；不合规项目标注与驳回；查看预审进度与AI成本明细；支持规则YAML版本化管理、迭代与复用；支持预算设置与降级策略。

2. 参赛团队需求（V1裁剪）：免费上传文本+链接进行自测，获取四类风险信号、核心短板与分优先级优化建议；**查阅当届生态/赛道适配与评审规则（YAML，可含示例生态片段）**；获取结构化自测报告（含证据）；查看自测历史记录，跟踪优化效果。

3. 评委需求（V1裁剪）：查看AI初筛后的项目列表、风险信号明细与完整证据；对高风险项目进行深度审核，标注审核意见；修正AI风险信号标注，标注复核原因并留存记录；查看当前赛事使用的规则版本（YAML），确保评审标准统一。

## 三、产品核心功能与需求规格（V1裁剪版，仅保留3件核心事）

### 3.1 整体功能架构（V1裁剪版）

系统分为前端用户交互层、后端核心服务层、核心功能层、数据层四大模块，V1仅聚焦3件核心事：项目提交（文本+链接）、规则加载（YAML）、红旗信号扫描与报告输出，轻量化设计，支持规则YAML版本化，适配小型Avalanche生态赛事，无需复杂部署，优先实现可落地性。

### 3.2 核心功能需求规格（V1裁剪版，贴合可落地）

#### 模块1：项目提交模块（V1核心，替代原项目管理模块）

功能描述：实现赛事项目（主办方端）、自测项目（参赛团队端）的提交与基础管理，仅支持文本+链接提交，PDF附件仅做存储，不做解析，为红旗信号扫描提供基础数据支撑，操作简洁，适配V1落地需求。

核心子功能：

1. 项目提交（V1输入规范，强制）：
       

    - 必填字段：project_title（项目名称）、one_liner（项目简介，≤200字）、problem（解决的问题）、solution（解决方案）；

    - 可选字段：why_this_chain（Avalanche生态适配理由，建议必填）、github_url（GitHub链接）、demo_url（演示链接）、docs_text（文本粘贴，可选）；

    - 附件支持：仅支持PDF上传，仅做原始附件存储，不保证全解析，不提取内容用于信号扫描；

    - 主办方端支持批量提交（文本批量导入+链接批量录入），参赛团队端支持单个项目提交。

2. 项目审核（主办方端）：对提交的参赛项目进行合规性审核（如是否符合Avalanche赛道要求、必填字段是否完整），通过/驳回并标注原因（关联对应风险信号），驳回项目支持参赛团队修改后重新提交；可批量审核。

3. 项目检索与分类：按预审状态（待预审、已预审、合格、不合格）、风险等级（致命/降分/不稳定/可优化）自动分类；支持按项目名称、团队名称、风险等级关键词精准检索，响应快速。

4. 自测历史（参赛团队端）：留存团队所有自测记录，关联对应规则版本、风险信号、证据与优化建议，支持查看历史自测报告，跟踪项目优化进度。

交互要求：提交界面简洁，必填字段标注清晰，支持文本粘贴、链接录入、PDF上传；检索、筛选操作便捷，一键切换风险等级分类。

非功能要求：支持千级项目同时存储，检索响应时间≤1s；文本/链接提交响应时间≤1s，PDF上传速度≤10MB/s；支持断点续传。

#### 模块2：可版本化规则引擎（V1裁剪版，核心架构）

功能描述：搭建“通用规则+Avalanche生态模块”的可版本化规则架构，V1仅支持YAML文件版本化+API读取生效，不做可视化配置，明确规则YAML最小可执行格式，为红旗信号扫描提供标准化规则支撑，确保工程可直接实现。

核心子功能：

1. 规则文件规范（V1最小可执行格式）：
       

    - 通用规则文件：rules/common.yaml，包含8个通用红旗维度，每条规则包含固定字段（id、when、signal_type、severity、evidence_fields、explain_template、fix_template）；

    - Avalanche生态规则文件：rules/ecosystem/avalanche.yaml，包含生态专属红旗维度与生态专属强信号，字段与通用规则一致；

    - 规则条件计算：明确条件判定逻辑（如GitHub commits数量、Demo链接可访问性、是否提到指定关键词+可验证依据等），确保工程可实现。

2. 规则加载与生效：系统通过API读取rules目录下的YAML文件，通用规则与Avalanche生态规则自动联动，无需手动配置；规则修改后，重新读取API即可生效，无需重启系统。

3. 版本化管理（V1简化版）：规则YAML文件按版本命名（如common_v1.0.yaml、avalanche_v1.0.yaml），主办方可替换YAML文件实现版本迭代、回溯；支持版本备注，标注版本更新日志（如规则调整、新增维度）；可复用历史YAML文件，避免重复开发。

4. 规则公开与Anti-gaming：主办方可选择将当前赛事的规则YAML文件公开给参赛团队；新增Anti-gaming规则，强信号仅出现关键词不算，需绑定“代码路径/交易hash/文档段落”之一作为可验证证据；明确证据门槛，所有强信号需可验证。

交互要求：规则YAML文件支持上传、替换、下载，版本标注清晰；规则公开界面支持YAML文件预览，明确提示强信号证据要求；规则加载状态可视化，加载失败给出明确提示。

非功能要求：规则加载响应时间≤1s；单规则YAML文件修改后，重新加载生效时间≤5s；规则判定逻辑可执行、可量化，无模糊表述；支持YAML文件格式校验，避免格式错误导致加载失败。

#### 模块3：红旗信号扫描与报告模块（V1核心，整合原AI辅助预审+团队自测+报告模块）

功能描述：采用“低成本AI初筛+人工深度复核”的模式，基于“通用规则+Avalanche生态模块”双架构，实现红旗信号扫描（Red Flag Scan）、四类风险信号分级输出、基础强信号识别，输出机器可读的结构化报告（含完整证据），新增预算与降级策略，规避API风险，适配V1落地需求。

核心子功能：

1. 红旗信号扫描（AI初筛）：对接DeepSeek等低成本AI API，根据加载的“通用规则+Avalanche生态模块”，对所有提交项目进行自动化扫描，识别四类风险信号、标注基础强信号，生成项目初筛结果（合格/不合格）；所有信号均包含完整证据，符合Evidence Schema规范。

2. Evidence Schema（证据结构，V1必填）：所有信号的证据需包含以下字段，确保可追溯、可验证：
        

    - field：证据来源字段（如github_url、docs_text、one_liner）；

    - quote：证据原文引用（截取相关文本/链接内容）；

    - pointer：精准定位（如链接路径、文本段落位置、代码路径、交易hash等）；

    - rule_id：关联的规则ID（对应YAML文件中的规则id）。

3. 风险信号分级输出（与原逻辑一致，明确处理建议）：
        

    - 致命淘汰信号（Kill Signal）：触发后直接判定为不合格，无需评委复核（如GitHub空壳、技术必要性为0、演示完全缺失）；

    - 降分信号（Score Down Signal）：不直接淘汰，但大幅降低预审优先级（如Buzzword堆砌、过度承诺）；

    - 不稳定信号（Volatility Risk）：评委需重点复核，存在主观判断差异（如商业路径模糊但有一定可行性、生态适配性一般）；

    - 可优化信号（Optimization Flag）：不影响初筛结果，仅提示优化方向（如强加分信号未充分展示、表述不够清晰）。

4. 强信号识别（V1简化版）：仅识别通用+Avalanche生态专属强信号，所有强信号需满足证据门槛（绑定可验证依据），标注信号类型、完整证据、加分依据；支持按强加分信号数量排序，优先展示优质项目。

5. 人工复核：支持评委对AI初筛结果进行深度复核，重点复核高风险项目、有争议的项目，可修改AI信号标注与初筛结果，标注复核原因并留存记录；支持批量复核。

6. 预算与降级策略（V1新增，必做）：
        

    - Budget exceeded（成本超支）：自动切换至最低成本模式，仅运行Kill Signals扫描，暂停降分、不稳定、可优化信号及强信号识别，直至调整预算上限；

    - API fail（第三方API故障）：自动切换至降级模式，用规则+正则+启发式算法，产出“低置信度报告”，标注报告置信度，提示用户API恢复后可重新扫描；

    - 成本监控：自动统计AI调用次数与成本，向主办方展示成本明细，支持设置AI调用上限，避免成本超出预期；优化AI调用逻辑，仅对未检测过的项目、修改后的项目进行调用。

7. 结构化报告输出（V1输出规范，机器可读）：
       

    - 输出格式：JSON格式（强制），同时支持导出PDF版本（包含JSON内容可视化）；

    - 输出规范（固定字段，不可缺失）：
                `{
      "summary": { "pass": true, "kill_count": 0, "score_down_count": 2, "volatility_count": 1, "opt_count": 3, "strong_signals": 4 },
      "signals": [
        {
          "type": "KILL|SCORE_DOWN|VOLATILITY|OPT|STRONG",
          "rule_id": "GITHUB_EMPTY_001",
          "severity": "HIGH|MED|LOW",
          "title": "GitHub 空壳信号",
          "evidence": [{ "field": "github_url", "quote": "仓库无contracts目录", "pointer": "repo: /contracts/ missing" }],
          "explanation": "该项目GitHub仓库缺少核心合约目录，符合GitHub空壳信号判定规则，属于致命淘汰信号",
          "fix": { "priority": 1, "actions": ["补充核心合约代码至contracts目录", "确保仓库提交次数≥5次", "提供代码路径说明"] }
        }
      ],
      "next_actions": ["优先修改致命风险信号（优先级1），补充GitHub核心代码；其次优化降分信号，明确商业路径；最后完善强信号展示，添加Avalanche Subnet相关代码路径"]
    }`

    - 主办方端：可导出单项目报告、批量导出赛事预审报告（含所有项目summary）；

    - 参赛团队端：可导出个人自测报告，查看信号明细、证据与优化建议。

8. 扫描日志与规则追溯：留存所有AI扫描记录、评委复核记录、项目信号变化、所用规则版本（YAML文件名），支持按项目检索查看；可查看每个信号对应的规则条款（YAML规则内容），便于争议处理。

交互要求：扫描进度可视化，风险信号与强信号分类标注（颜色区分：致命=红色、降分=橙色、不稳定=黄色、可优化=蓝色、强加分=绿色）；评委复核界面清晰，支持快速查看项目提交内容、AI标注内容与完整证据；成本明细、API状态直观展示，降级时给出明确提示；报告导出便捷，支持JSON/PDF双格式。

非功能要求（V1整改，规避高风险承诺）：效率指标：千级项目扫描完成时间≤2小时（可配置预算上限），单项目扫描响应时间≤3s；质量指标：用可落地指标替代绝对准确率——评委每项目平均复核时间下降≥30%、Kill Signal误杀率≤5%、Top-20优质项目召回率≥80%；稳定性指标：报告生成时间≤10s，批量导出千份报告≤30分钟；API降级切换响应时间≤5s，不影响系统运行；数据指标：证据完整率100%，所有信号均包含符合规范的证据；规则版本追溯响应时间≤1s。

#### 模块4：系统管理模块（V1裁剪版，仅保留基础支撑功能）

功能描述：实现系统的基础配置、用户管理、权限控制、数据监控、规则YAML管理，适配V1核心功能运行，确保系统稳定，降低运维成本。

核心子功能（V1裁剪）：

1. 用户管理：支持主办方创建评委账号；支持参赛团队自主注册、登录，绑定团队信息；支持用户信息修改、密码重置；支持按角色分组管理。

2. 权限控制：基于角色的权限管理（RBAC），主办方拥有全权限，评委仅可查看、复核项目与扫描报告，参赛团队仅可操作自测相关功能；确保数据安全。

3. 赛事配置：主办方可创建/编辑/结束赛事，配置赛事名称、Avalanche赛道、预审时间、规则版本（选择对应YAML文件）、合格阈值等；可复制历史赛事配置。

4. 规则YAML管理：支持通用规则、Avalanche生态规则YAML文件的上传、替换、下载、版本标注；支持查看规则使用记录。

5. API管理：对接DeepSeek等低成本AI API，支持API密钥配置、调用次数统计、容错处理（关联降级策略）；预留备用接口，支持接口调用日志留存。

6. 数据安全：所有用户数据、项目提交内容、扫描记录、报告、规则YAML文件进行加密存储，支持数据每日自动备份；支持数据恢复，防止数据泄露、篡改与丢失。

交互要求：系统管理界面简洁，操作便捷；规则YAML管理界面支持文件上传、替换，版本标注清晰；API配置、预算设置界面直观，降级策略触发时给出明确提示。

非功能要求：系统7*24小时稳定运行，故障恢复时间≤30分钟；数据备份每日自动进行，可随时恢复；规则YAML操作响应时间≤1s；支持200+用户同时在线操作，无卡顿。

## 四、非功能需求（V1裁剪版，贴合可落地）

### 4.1 性能需求（V1整改，规避高风险承诺）

1. 系统并发能力：单服务器支持千级项目同时存储与处理，支持200+用户同时在线操作（主办方、参赛团队、评委）；支持单个Avalanche生态赛事运行，无卡顿。

2. 响应时间：核心操作（项目提交、红旗信号扫描、报告生成、检索、规则加载）响应时间≤3s；API降级切换响应时间≤5s；规则YAML加载响应时间≤1s。

3. 处理效率：千级项目红旗信号扫描完成时间≤2小时（可配置预算上限），单项目扫描≤3s，赛事预审总报告生成≤10分钟；批量导出千份报告≤30分钟。

### 4.2 兼容性需求（V1裁剪版）

1. 前端兼容：支持主流浏览器（Chrome、Firefox、Edge、Safari），兼容电脑端，适配平板端操作，无需安装客户端，浏览器直接访问；界面适配不同屏幕尺寸，无排版错乱。

2. 接口兼容：对接的AI API支持主流版本，支持API参数灵活配置，预留备用接口；支持YAML文件格式兼容（主流编辑工具可编辑）。

3. 数据兼容：支持PDF文件上传存储，支持JSON/PDF格式报告导出，确保文件正常打开；支持文本粘贴（兼容各类纯文本格式）、链接录入（主流链接格式）。

### 4.3 稳定性需求（V1新增降级相关）

1. 系统可用性：全年可用性≥99.9%，黑客松预审高峰期（赛事报名结束后1-3天）可用性≥100%；API故障时可自动降级，不影响核心扫描功能（仅Kill Signals）。

2. 故障处理：系统出现异常时自动报警，留存故障日志，支持快速排查与恢复，故障恢复时间≤30分钟；AI接口故障时，自动切换备用接口或降级模式，确保核心功能可用。

3. 数据安全：所有用户数据、项目内容、扫描记录、规则YAML、报告进行加密存储，防止数据泄露、篡改与丢失；支持数据备份与恢复，用户操作日志留存，便于异常追溯。

### 4.4 可扩展性需求（V1简化，预留后续迭代空间）

1. 功能扩展：系统架构支持后续新增生态模块（如Solana、ETH）、打分功能、付费服务，无需大规模改造核心代码；支持规则YAML字段扩展，适配后续规则迭代。

2. 接口扩展：支持对接更多第三方AI API，接口层设计标准化，扩展便捷；预留链上数据查询接口，适配后续Web3生态强信号识别需求。

3. 部署扩展：支持SaaS云端部署（中小主办方首选）、私有化本地部署，部署流程简单，运维成本低；支持服务器集群扩展，适配后续大型赛事需求。

### 4.5 易用性需求（V1聚焦简洁）

1. 学习成本：前端界面设计简洁直观，所有角色用户零学习成本即可上手操作；项目提交、规则上传、报告导出流程简单，无需专业技术知识。

2. 操作便捷：核心功能（项目提交、红旗信号扫描、报告导出、规则上传）实现一键操作，减少繁琐步骤；参赛团队自测操作简洁，无需复杂配置。

3. 提示清晰：系统操作过程中给出明确的引导提示、错误提示，API降级、成本超支时给出明确提醒；风险信号、证据、优化建议清晰易懂，可落地。

## 五、产品技术架构要求（V1裁剪版，贴合工程落地）

系统采用轻量化分层架构，兼顾高性能、高并发与低运维成本，以“通用规则+Avalanche生态模块（YAML）”为核心架构，贴合中小主办方与参赛团队的使用场景，支持规则YAML版本化，实现红旗信号扫描核心功能，核心技术架构要求如下：

1. 前端层：基于HTML/CSS/JS开发，轻量化设计，无需安装客户端，浏览器直接访问；界面简洁，适配多设备操作；重点实现项目提交、报告查看、规则上传功能，无需复杂交互。

2. 后端层：基于Golang开发，实现高性能、高并发处理，单服务器支撑千级项目扫描与管理，降低部署与运维成本；支持多线程处理AI扫描任务，提升处理效率；实现规则YAML文件的读取、解析与版本管理。

3. 核心逻辑层：包含可版本化规则引擎（YAML解析）、红旗信号扫描模块，模块解耦，独立部署与迭代；核心逻辑与规则分离，规则修改（替换YAML）无需改动核心代码，提升迭代效率；实现预算控制与降级策略逻辑。

4. 第三方接口层：对接DeepSeek等低成本AI API，接口层做容错处理，预留备用接口，实现降级策略联动；支持API密钥灵活配置、调用次数统计；支持接口调用日志留存，便于排查故障。

5. 数据层：存储用户数据、项目提交内容、扫描记录、报告、规则YAML文件等，支持数据加密与每日自动备份；采用轻量化数据库，降低部署成本；实现规则YAML文件的结构化存储，支持快速读取与版本管理；支持数据关联查询（如项目与规则版本、信号证据的关联）。

6. 部署要求：支持云端SaaS部署（一键开通使用）、私有化本地部署（提供标准化部署流程），主办方无需专业技术团队即可维护；支持规则YAML文件在线上传、替换，无需重启系统；支持单个Avalanche生态赛事部署，无复杂配置。

## 六、产品规划与迭代路线（整改版，明确各版本重点，规避V1高风险）

严格遵循“先做强防守（红旗信号扫描）、再做进攻（强信号制造+改写）、再完善评分、最后做排序”的战略顺序，分四阶段迭代，聚焦可落地、可实战，避免同时开发多模块，确保每阶段有明确的实战价值，V1优先落地核心可执行功能，后续逐步扩展。

### V1.0（MVP，1周内上线）—— 防守核心：红旗信号扫描（Red Flag Scan）

核心目标：验证“通用红旗规则（YAML）+Avalanche生态模块（YAML）”的可行性，实现四类风险信号分级识别、完整证据输出，支持预算控制与API降级，帮助主办方快速排雷、参赛团队规避致命风险，适配小型Avalanche生态黑客松赛事，快速落地试点，收集用户反馈。

上线功能（仅3件核心事，其余全砍掉）：

1. 项目提交（文本+链接，必填字段严格遵循V1输入规范，PDF仅存储不解析）；

2. 规则加载（YAML：通用规则8大维度+Avalanche生态模块，支持YAML上传、替换、版本化，API读取生效）；

3. 红旗信号扫描与报告输出（AI初筛+人工复核，输出机器可读JSON报告，含完整Evidence Schema，支持JSON/PDF导出；新增预算控制与API降级策略；规避绝对准确率指标，采用可落地的复核效率、误杀率、召回率指标）；

4. 基础系统管理（用户管理、简单赛事配置、规则YAML管理、API配置、数据安全）。

不做功能：可视化规则编辑、PDF/PPT解析、打分、排序、付费、一对一指导、复杂数据看板、跨生态适配、强信号制造改写。

成功标准（V1整改，可落地）：
      效率：千级项目扫描完成时间≤2小时（可配置预算上限），用户可快速上手；质量：评委每项目平均复核时间下降≥30%、Kill Signal误杀率≤5%、Top-20优质项目召回率≥80%；证据完整率100%；稳定性：API故障时可正常降级，核心扫描功能（Kill Signals）可用；系统无崩溃，故障恢复时间≤30分钟；工程适配：规则YAML可直接解析，代码可落地，无模糊需求；报告输出符合JSON规范，可机器读取。

### V2.0（1个月内上线）—— 进攻核心：新增阶段四（Surgical Rewrite Engine）

核心目标：完善风险排雷能力，新增强信号制造、针对性改写功能，实现“防守+进攻”双重能力，支持多生态模块（Solana、ETH、AI）切换（YAML方式），适配中型赛事，提升参赛团队项目竞争力；新增规则可视化配置。

新增/迭代功能：

1. 可版本化规则引擎迭代（新增Solana、ETH、AI 3个生态模块YAML，完善规则版本管理、导入/导出功能，支持生态模块YAML一键切换；新增规则可视化配置界面）；

2. 红旗信号扫描迭代（优化风险信号判定逻辑，提升复核效率与召回率；完善强信号识别，丰富强信号类型，严格执行证据门槛；优化预算控制与降级策略）；

3. 新增阶段四核心功能（Surgical Rewrite Engine）：针对风险信号与强信号缺失，提供三段式改写（批判标注、结构性建议、可复制粘贴的替换版本），确保改写后风险降低、强信号突出，红旗信号减少≥50%；

4. 报告模块迭代（支持报告定制、批量导出，优化报告内容，新增强信号明细、改写建议）；

5. 系统管理迭代（数据看板、多赛事管理、权限精细化配置、生态模块YAML管理）。

成功标准：改写后项目红旗信号减少≥50%，强信号数量提升≥30%；多生态模块切换流畅（YAML方式），响应≤1s；参赛团队优化后自测通过率提升≥20%。

### V3.0（2个月内上线）—— 完善核心：新增阶段二（Structured Judge Engine）

核心目标：新增标准化打分功能，实现“风险排雷+强信号制造+精准打分”，优化AI模型准确率，适配大型赛事，提升评分稳定性，避免分数漂移；新增付费增值服务。

新增/迭代功能：

1. 新增阶段二核心功能（Structured Judge Engine）：搭建标准化打分维度（创新性、技术完成度、生态对齐度、可落地性、演示清晰度、长期建设信号），每个维度给出分数、原文证据、扣分/加分理由，确保分数稳定、不漂移；

2. AI模型迭代（提升风险信号、强信号识别准确率至≥90%，优化打分逻辑，确保分数稳定，同类型项目分数偏差≤5%）；

3. 可版本化规则引擎迭代（新增更多生态模块，支持自定义生态模块YAML，完善规则模板库）；

4. 报告迭代（新增打分明细、证据标注，完善改写建议，支持跨生态打分对比）；

5. 新增付费增值服务（深度自测报告、一对一导师指导、改写定制服务）；

6. 系统性能优化（提升并发能力，支持万级项目处理，优化响应速度）。

成功标准：打分稳定性达标（同类型项目分数偏差≤5%），打分证据完整率100%；AI识别准确率≥90%；支持万级项目处理，核心操作响应≤3s；付费服务有明确转化。

### V4.0（3个月内上线）—— 闭环核心：新增阶段三（Cutline Simulation Engine）

核心目标：新增博弈排序功能，实现“风险排雷+强信号制造+精准打分+博弈排序”；基于前3个版本积累的赛事项目池分布、同赛道基线数据，实现精准排序，辅助主办方确定入围名单；完善跨生态适配能力。

新增/迭代功能：

1. 新增阶段三核心功能（Cutline Simulation Engine）：基于赛事项目池分布、同赛道基线数据，实现博弈排序，支持主办方设置排序权重，精准筛选入围项目；

2. 完善跨生态适配能力，支持生态模块无缝切换（可视化操作），实现同一项目跨生态快速适配、对比；

3. AI模型迭代（提升排序准确性，优化信号识别与打分逻辑）；

4. 报告迭代（新增排序明细、基线对比、入围概率分析）；

5. 系统优化（提升并发能力，完善数据看板，支持大型赛事多生态同时运行）。

成功标准：排序准确性≥90%，与评委人工排序一致性≥85%；跨生态适配流畅，支持万级项目多生态同时处理；主办方入围筛选效率提升≥80%。

## 七、V1关键可执行资产（补充缺失，工程可直接使用）

### 7.1 V1 规则YAML最小可执行格式（必选，工程直接解析）

#### 7.1.1 rules/common.yaml（通用规则，8个红旗维度）

```yaml
# 通用红旗规则YAML，V1.0版本
version: "1.0"
rules:
  - id: GITHUB_EMPTY_001  # 规则唯一ID
    name: GitHub空壳信号
    when:  # 条件判定（可执行，工程可解析）
      - field: github_url
        condition: "exists == true"
      - field: github_url
        condition: "repo_create_time > 赛事报名时间"
      - field: github_url
        condition: "commit_count < 5"
    signal_type: KILL  # 信号类型：KILL/SCORE_DOWN/VOLATILITY/OPT/STRONG
    severity: HIGH  # 严重程度：HIGH/MED/LOW
    evidence_fields: ["github_url"]  # 证据来源字段
    explain_template: "该项目GitHub仓库创建时间晚于赛事报名时间，且提交次数＜5次，属于空壳仓库，触发致命淘汰信号。"  # 解释模板
    fix_template:  # 优化建议模板
      priority: 1  # 优先级（1最高，3最低）
      actions:
        - "补充核心代码至GitHub仓库，确保提交次数≥5次"
        - "提供仓库代码路径说明，证明项目真实性"
        - "确保GitHub仓库可正常访问，无权限限制"

  - id: TECH_NECESSITY_002
    name: 技术必要性不足信号
    when:
      - field: solution
        condition: "not contains 技术方案/技术架构/核心技术"
      - field: problem
        condition: "not contains 痛点/需求场景"
    signal_type: SCORE_DOWN
    severity: MED
    evidence_fields: ["solution", "problem"]
    explain_template: "该项目解决方案中未明确技术方案、技术架构或核心技术，且未清晰说明解决的痛点与需求场景，技术必要性不足，触发降分信号。"
    fix_template:
      priority: 2
      actions:
        - "在解决方案中补充核心技术架构、关键技术选型及理由"
        - "明确说明项目解决的具体痛点、目标用户及需求场景"
        - "补充技术方案的可行性分析，体现技术价值"

  # 其余6个通用红旗规则（Buzzword堆砌、过度承诺、商业路径模糊、伪需求、演示缺失、不可验证）按上述格式补充，确保when条件可执行

```

